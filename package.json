{
  "name": "show-tracking-ids",
  "version": "1.0.0",
  "scripts": {
    "build": "gulp",
    "now-build": "npm run build",
    "start": "web-dev-server --node-resolve --watch --root-dir ./dist --open"
  },
  "dependencies": {},
  "devDependencies": {
    "@web/dev-server": "^0.1.8",
    "del": "^1.1.1",
    "gulp": "^4.0.2",
    "gulp-babel-minify": "^0.5.0",
    "gulp-concat": "^2.1.6",
    "gulp-util": "^3.0.0"
  },
  "private": true
}
